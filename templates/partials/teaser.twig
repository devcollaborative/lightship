<div class="teaser">
  <h3><a href="{{ post.link }}">{{ post.title }}</a></h3>
  <img src="{{ post.thumbnail|resize(400, 300) }}" alt="{{ post.thumbnail.alt }}">
  <p>{{ post.excerpt.length(50).read_more(false) }}</p>
</div>