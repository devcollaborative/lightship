{% if posts.pagination is not empty %}
  <nav class="navigation pagination" aria-label="Posts">
		<h2 class="screen-reader-text">Posts navigation</h2>

		<div class="nav-links">
      {% if posts.pagination.prev %}
        <a class="previous page-numbers" href="{{ posts.pagination.prev.link }}">Previous</a>
      {% endif %}

      {% for page in posts.pagination.pages %}
        {% if page.link %}
          <a class="page-numbers" href="{{ page.link }}">{{ page.title }}</a>
        {% else %}
          <span aria-current="page" class="page-numbers current">{{ page.title }}</span>
        {% endif %}
      {% endfor %}

      {% if posts.pagination.next %}
        <a class="next page-numbers" href="{{ posts.pagination.next.link }}">Next</a>
      {% endif %}
    </div>
	</nav>
{% endif %}
